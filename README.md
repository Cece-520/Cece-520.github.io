# Cece-520.github.io
Anh Pham Portfolio

## Customization
- **Cat options:** In the projects timeline footer, you can switch the cat by editing the `cat-zone` in [index.html](index.html). Available elements:
	- `div.cat` (default, black cat emoji 🐈‍⬛)
	- `div.cat-alt` (alternate emoji 🐱)
	- `svg.cat-svg` (simple SVG cat reaching toward the yarn)

To use an alternative, replace the `cat` element with `cat-alt` or `cat-svg` and optionally remove the others. The hover animation applies to all options.
